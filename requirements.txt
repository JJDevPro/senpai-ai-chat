# Senpai AI Coach — Python deps for the Claude Code (web) VM.
# Installed by the cloud env setup script; the SessionStart hook is a fallback.
# INSTALL WITH:  pip install --use-pep517 -r requirements.txt
#   fitparse ships no PyPI wheel; its legacy setup.py build fails on Ubuntu 24.04's
#   Debian-patched setuptools (AttributeError: install_layout) unless build isolation
#   (--use-pep517) is forced. The cloud-env Setup-Script should install inline:
#   pip install --use-pep517 pandas numpy fitparse google-api-python-client google-auth
pandas>=2.0              # truth-sheet + series reduction
numpy>=1.24             # numerics for banister / aggregates
fitparse>=1.2.0         # decode Garmin/HealthFit .fit files
google-api-python-client>=2.0   # Drive read-only pulls
google-auth>=2.0        # service-account auth for Drive
cffi>=1.15              # cryptography backend for google-auth signing; the VM ships a
                        # Debian `cryptography` that satisfies pip's resolver, so cffi is
                        # never pulled into the python3.11 site-packages on its own and the
                        # Drive pull dies with "No module named '_cffi_backend'". Pin it.
scipy>=1.10             # robust z-score / least-squares for stats.py (banister_fit, anomaly)
statsmodels>=0.14       # OLS + 95%-CI for stats.py bedtime_hrv / race_readiness
                        # NOTE: scipy+statsmodels pull large wheels (scipy ~40 MB, plus
                        # patsy) — they MEASURABLY enlarge the cloud setup-script install.
                        # Only stats.py needs them; the core daily-check runs without.
