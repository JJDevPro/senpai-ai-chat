[pytest]
# Regression suite for the skill scripts under .claude/skills/*/scripts/.
# All fixtures are synthetic (DATA-FREE repo) — no real health data is committed.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
