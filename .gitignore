# Ephemeral Drive downloads — never committed (Drive is the source of truth)
data/

# Python / venv
.venv/
__pycache__/
*.py[cod]

# OS / editors
.DS_Store
.idea/
.vscode/

# Secrets — Drive service-account creds stay out of git
service-account*.json

# Local Claude Code overrides
.claude/settings.local.json
